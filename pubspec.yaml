name: voice_our_iraq_app
description: "Ahl Aldira voice chat app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  zego_uikit_prebuilt_call:
  zego_uikit_signaling_plugin:
  permission_handler: ^12.0.3
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true